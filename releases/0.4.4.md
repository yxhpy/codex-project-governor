# 0.4.4 — Project Hygiene Doctor

## Added

- `project-hygiene-doctor` skill.
- `inspect_project_hygiene.py` helper.
- Project hygiene policy template.
- Hygiene prompt template.
- Clean init profile in `tools/init_project.py`.

## Changed

- Initialization now defaults to a clean profile that copies only project-owned governance files.
- Default `.codex/agents`, `.codex/prompts`, and `.codex/config.toml` remain plugin-owned unless explicitly installed through the legacy-full profile.
