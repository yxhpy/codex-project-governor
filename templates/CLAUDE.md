@AGENTS.md

## Claude Code

Use the installed Project Governor Claude Code plugin automatically for governed project work when available. Do not wait for the user to invoke `/pg-*` commands.

- Treat natural coding, testing, review, docs, upgrade, initialization, compatibility, quality, memory, and UI requests as Project Governor-governed work.
- Read the imported `AGENTS.md` rules first, then route, retrieve context, run quality checks, use memory, and apply DESIGN.md gates automatically when the task requires them.
- Use `/pg-*` commands only as optional shortcuts or diagnostics for a specific workflow stage.

Keep this file short; detailed shared rules live in `AGENTS.md`.
