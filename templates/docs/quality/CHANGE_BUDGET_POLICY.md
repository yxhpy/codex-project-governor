# Change Budget Policy

Every task should declare max files changed, new files, dependency changes, public contract changes, schema changes, styling changes, and refactor allowance before implementation.
