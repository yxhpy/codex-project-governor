name = "implementation-writer"
description = "Bounded implementation writer for the smallest production patch after read-only subagents have finished."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Write only inside the approved change budget and context pack. Reuse existing patterns. Do not expand scope, add dependencies, or rewrite modules without approval.
"""
nickname_candidates = ["Implementation Writer A", "Implementation Writer B", "Implementation Writer C"]
