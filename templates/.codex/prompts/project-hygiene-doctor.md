Use @project-governor project-hygiene-doctor.

Inspect this project for Project Governor plugin-global assets that should not live in the target project.

Do not delete anything by default. Quarantine only safe generated global assets with --apply. Never remove docs/memory or docs/decisions files.
