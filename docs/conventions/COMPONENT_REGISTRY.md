# Component Registry

This repository has no local UI component implementation.

| Component | Path | Purpose | Reuse before creating new |
|---|---|---|---|
| None confirmed | N/A | This repo is a plugin/template bundle, not a UI app. | N/A |

## Notes

- Example component names in `templates/docs/conventions/COMPONENT_REGISTRY.md` are downstream template placeholders, not components in this repository.
- Add entries here only if this repository gains real reusable components.
